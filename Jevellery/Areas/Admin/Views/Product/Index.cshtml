@model List<Product>

<div class="card">
    <div class="card-body">
        <div class="d-flex">
            <h5 class="card-title">Categories</h5>
            <div class="col-2">
                <a asp-action="create" asp-controller="product" asp-area="admin" class="btn btn-outline-success">Add product</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-sm">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Name</th>
                        <th scope="col">Description</th>
                        <th scope="col">Image</th>
                        <th scope="col">Price</th>
                        <th scope="col">Discount</th>
                        <th scope="col">CategoryName</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <th>@product.Id</th>
                            <td>@product.Name</td>
                            <td class="shorten-text">@product.Description</td>
                            <td class="admin-first-img">
                                <img src="@product.Filename" />
                            </td>
                            <td>@product.Price</td>
                            <td>@product.Discount</td>
                            <td>@product.Category.Name</td>
                            <td>
                                <a class="btn btn-outline-warning text-white" asp-action="edit" asp-controller="product" asp-area="admin" asp-route-id="@product.Id">Edit</a>
                                <a class="btn btn-outline-danger text-white" asp-action="delete" asp-controller="product" asp-area="admin" asp-route-id="@product.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>